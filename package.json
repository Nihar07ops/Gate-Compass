{
  "name": "gate-cse-prep-platform",
  "version": "1.0.0",
  "description": "GATE CSE Preparation Platform with ML-powered predictions",
  "scripts": {
    "install-all": "npm install && cd client && npm install && cd ../server && npm install",
    "dev": "concurrently \"cd server && npm run dev\" \"cd client && npm run dev\"",
    "build": "cd client && npm run build",
    "start": "cd server && npm start"
  },
  "keywords": ["gate", "cse", "mern", "ml"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
